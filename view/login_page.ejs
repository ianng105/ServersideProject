<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
  <title><%= title || 'login page' %></title>

  <!-- 使用你已有的 CSS；如文件名不同，通过渲染参数传入 cssHref 或直接改成你的路径 -->
  <link rel="stylesheet" href="<%= cssHref || '/css/login_page.css' %>">
</head>
<body>
  <main class="login-page container">
    <h1><%= pageHeading || 'login page' %></h1>

    <section class="login-card">
      <h2><%= loginTitle || 'Login' %></h2>

      <form class="login-form" action="<%= formAction || '/login' %>" method="<%= formMethod || 'POST' %>">
        <div class="form-field">
          <label for="email"><%= emailLabel || 'Email address' %></label>
          <input type="email" id="email" name="email" placeholder="<%= emailPlaceholder || 'Email address' %>" required />
        </div>

        <div class="form-field">
          <label for="password"><%= passwordLabel || 'Password' %></label>
          <input type="password" id="password" name="password" placeholder="<%= passwordPlaceholder || 'Password' %>" required />
        </div>

        <button type="submit" class="btn primary"><%= loginButton || 'Login' %></button>
      </form>

      <div class="divider">
        <span><%= orText || 'OR' %></span>
      </div>

      <div class="social-login">
        <a class="btn facebook" href="<%= facebookHref || '/auth/facebook' %>">
          <%= facebookText || 'Continue through Facebook' %>
        </a>
      </div>

      <p class="privacy-note"><%= privacyNote || 'We will not disclose any information without your permission.' %></p>
    </section>
  </main>

  <!-- 你的 JS，如没有可不传 -->
  <% if (jsSrc) { %>
    <script src="<%= /js/login_page.js %>"></script>
  <% } %>
</body>
</html>
