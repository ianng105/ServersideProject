<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile - HealthyPal</title>
  <link rel="stylesheet" href="/css/register.css"> <!-- 复用注册页样式 -->
</head>
<body>
  <div class="container">
    <div class="topbar">
      <h1 class="title">Edit Profile</h1>
    </div>

    <form action="/updateProfile" method="POST" enctype="multipart/form-data">
    <!-- 在 editProfile.ejs 的 form 中添加以下内容 -->
    <div class="field">
      <label class="label" for="height">Height (cm)</label>
      <input type="number" id="height" name="height" 
            value="<%= user.height || '' %>" class="input">
    </div>

    <div class="field">
      <label class="label" for="weight">Weight (kg)</label>
      <input type="number" id="weight" name="weight" 
            value="<%= user.weight || '' %>" class="input">
    </div>

    <div class="field">
      <label class="label" for="gender">Gender</label>
      <select id="gender" name="gender" class="input">
        <option value="">Select</option>
        <option value="male" <%= user.gender === 'male' ? 'selected' : '' %>>Male</option>
        <option value="female" <%= user.gender === 'female' ? 'selected' : '' %>>Female</option>
      </select>
    </div>

    <div class="field">
      <label class="label" for="birthday">Birthday</label>
      <input type="date" id="birthday" name="birthday" 
            value="<%= user.birthday || '' %>" class="input">
    </div>

    <div class="field">
      <label class="label" for="activity">Activity Level</label>
      <select id="activity" name="activity" class="input">
        <option value="">Select</option>
        <option value="sedentary" <%= user.activity === 'sedentary' ? 'selected' : '' %>>Sedentary</option>
        <option value="light" <%= user.activity === 'light' ? 'selected' : '' %>>Light</option>
        <option value="moderate" <%= user.activity === 'moderate' ? 'selected' : '' %>>Moderate</option>
        <option value="active" <%= user.activity === 'active' ? 'selected' : '' %>>Active</option>
        <option value="very_active" <%= user.activity === 'very_active' ? 'selected' : '' %>>Very Active</option>
      </select>
    </div>

    <div class="field">
      <label class="label" for="goal">Fitness Goal</label>
      <select id="goal" name="goal" class="input">
        <option value="">Select</option>
        <option value="lose" <%= user.goal === 'lose' ? 'selected' : '' %>>Lose Fat</option>
        <option value="gain" <%= user.goal === 'gain' ? 'selected' : '' %>>Gain Muscle</option>
        <option value="maintain" <%= user.goal === 'maintain' ? 'selected' : '' %>>Maintain Weight</option>
      </select>
    </div>

      <div class="field">
        <label class="label" for="username">Username</label>
        <input type="text" id="username" name="newUsername" 
               value="<%= user.username %>" class="input" required>
      </div>

      <div class="field">
        <label class="label" for="profile">Profile Description</label>
        <textarea id="profile" name="profile" class="input" 
                  style="height: 120px;"><%= user.profile %></textarea>
        <div class="hint">Tell others about your fitness journey</div>
      </div>

      <div class="field">
        <label class="label" for="avatar">Profile Picture</label>
        <input type="file" id="avatar" name="avatar" accept="image/*" class="input">
        <div class="hint">Upload a square image (recommended 400x400px)</div>
        <% if (user.avatar) { %>
          <div style="margin-top: 10px;">
            <img src="<%= user.avatar %>" alt="Current avatar" style="width: 80px; border-radius: 50%;">
          </div>
        <% } %>
      </div>

      <div class="footer-bar">
        <button type="button" class="fab-back" onclick="window.location.href='/userProfile'">
          <i>←</i>
        </button>
        <button type="submit" class="cta">Save</button>
      </div>
    </form>
  </div>
</body>
</html>