<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
  <title><%= title || 'HealthyPal' %></title>

  <!-- 引用你已有的样式文件：根据你的文件名修改路径 -->
  <link rel="stylesheet" href="<%= cssHref || '/css/login.css' %>">
</head>
<body>
  <main class="wrap">
    <div class="badge"><%= appLabel || 'app' %></div>

    <h2><%= welcome || 'Welcome to use' %></h2>
    <h1><%= product || 'HealthyPal' %></h1>

    <p class="lead"><%= tagline || 'Are you ready to start? Tracking begins now!' %></p>

    <% const list = Array.isArray(bullets) && bullets.length ? bullets : [
      'Understand the effects of diet and exercise',
      'Make mindful eating a long-term habit'
    ]; %>
    <ul class="features">
      <% list.forEach(item => { %>
        <li><%= item %></li>
      <% }) %>
    </ul>

    <div class="cta">
      <a class="btn primary" href="<%= registerHref || '/register' %>"><%= registerText || 'Register For Free' %></a>
      <a class="btn" href="<%= loginHref || '/login' %>"><%= loginText || 'Login' %></a>
    </div>

    <footer class="footer">
      Version <%= version || '1.0.0' %>
    </footer>
  </main>

  <!-- 引用你已有的脚本文件：根据你的文件名修改路径 -->
  <script src="<%= jsSrc || '/js/login.js' %>"></script>
</body>
</html>
